application: <%= _.slugify(appId) %>
version: 0
runtime: python27
api_version: 1
threadsafe: yes

inbound_services:
- warmup

libraries:
- name: webapp2
  version: latest
- name: jinja2
  version: latest

handlers:
- url: /.*
  script: app.application
